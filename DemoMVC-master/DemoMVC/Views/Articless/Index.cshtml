@model IEnumerable<DemoMVC.Models.Articles>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Articlescontent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Author)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Articlescontent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArticlesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArticlesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArticlesID })
        </td>
    </tr>
}

</table>
